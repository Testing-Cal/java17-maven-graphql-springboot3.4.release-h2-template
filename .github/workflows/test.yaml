name: Test

on: [workflow_dispatch]

jobs:
  Test:
    runs-on: ubuntu-latest

    steps:
      - name: Initialization
        run: echo "hello"

      - name: Post Action
        if: always()
        run: |
          echo "post action"
          
          
